标志位寄存器中保存的当前的指令运算的状态信息。一个状态位寄存器中可以包含多种有效信息，其中常见的标志位有cf(表示是否进位，占了8位) of(表示是否溢出，占了8位)。对于无符号的整型，不存在溢出，只有可能存在进位，而对于有符号的整型，CPU一般可以通过最高位的符号位的进位和次高位的数字进位这两个数进行异或，如果异或的结果是1就表示出现了溢出（其中如果是符号位进位，而次高位没有进位就是负数出现了溢出，如果是符号位没有进位而次高位出现了进位就是正数的溢出）。理论上可以使用汇编语言取出寄存器进行判断，然后将最终的结果进行修正并用表示能力更强的类型进行保存。